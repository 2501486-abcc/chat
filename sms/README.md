SMS機能は、ログインを必要としない匿名チャット機能です。
WebSocket通信を用いて、接続したユーザー同士を自動的に1対1でマッチングし、リアルタイムにメッセージを送受信できます。

UIは一般的なメッセージアプリ（WhatsApp / iMessage など）の会話表示を参考に、送信（右寄せ）・受信（左寄せ）を色と配置で区別しています。配色は既存のカラーパレット（`--pink-accent`, `--pink-hover`, `--bg-main`, `--bg-card`, `--brown-*`）を流用しています。